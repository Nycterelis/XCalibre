@using Microsoft.AspNet.Identity;

@model IEnumerable<XCalibre.Models.Project>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>
    @*@if (User.IsInRole("Admin") || User.IsInRole("ProjectManager"))*@
    {
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id }) |
                @Html.ActionLink("Edit Users", "EditProjectUser", "AdminProjectView", new { id = item.Id }, null) |
                @Html.ActionLink("Assign to a Project Manager", "EditProjectManager", "AdminProjectView", new { id = item.Id }, null)
            </td>
        </tr>
    }

    }
    @Html.ActionLink("Test", "MyProjects", "UserPersonal", new { userId = User.Identity.GetUserId() }, null)

</table>
